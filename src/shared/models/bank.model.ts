export interface BankModel {
  id: string;
  name: string;
  short: string; // initials shown inside the logo badge
  color: string; // brand color (approximate)
  textColor: string; // badge text color for contrast
}
