import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xaviel.xmoney',
  appName: 'X Money',
  webDir: 'dist/x-money/browser',
  backgroundColor: '#121212',
};

export default config;
